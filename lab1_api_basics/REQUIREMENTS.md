# Соответствие лабораторной № 1

| Элемент первого семинара | Реализация | Артефакт после запуска |
|---|---|---|
| Первый API-запрос и usage | `experiments.py`, этап `hello` | `hello.json`, `api_trace.jsonl` |
| Собственный текст и стоимость | `OWN_TEXT`, tiktoken | `token_estimate.json` |
| T=0 против T=1,0, N=10 | 20 отдельных запросов | `temperature.json`, PNG |
| Лестница промптинга | zero-shot, role, format, few-shot | `prompt_experiments.json` |
| Prompt golf | 5 разных заголовков | доля чистых цифр 0–5 |
| Битва персон | профессор, друг, консультант, ребёнок | 4 полных ответа |
| Оценщик заголовков | 20 заголовков × 10 повторов | CSV, JSON, PNG |
| Воспроизводимость | кэш, журнал, лимиты, проверка | `run.json`, `budget.json`, verifier |

В исходном репозитории нет отдельного файла домашнего задания для семинара 1. Объём восстановлен по комментариям и TODO в шести стартовых скриптах. Результаты шаблона преподавателя не выдаются за собственный прогон.
