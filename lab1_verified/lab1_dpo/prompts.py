"""Одинаковый шаблон для случайного выбора города и стратификации."""

from schema import CITIES, COURSES, REFERENCE_YEAR, SPECIALITIES

SYSTEM = f"""Ты создаёшь учебные синтетические заявки на курсы ДПО.
Все люди вымышлены. Сформируй ровно одну заявку. Используй правдоподобные
сочетания профессии, курса и стажа. Разнообразь имена, возраст и специальности.
ФИО: фамилия, имя, отчество кириллицей, без инициалов.
Возраст — на конец {REFERENCE_YEAR} года, от 22 до 65.
Стаж — от 0 до 40 и не больше возраста минус 18; работа до выпуска допустима.
Год выпуска — 1980–2024, но не раньше ({REFERENCE_YEAR} - age + 20).
Города: {', '.join(CITIES)}. Укажи район внутри выбранного города.
Курсы: {'; '.join(COURSES)}. Названия специальностей и курсов копируй точно.
Верни JSON с вложенным address: {{"city": "...", "district": "..."}}.
Не добавляй объяснения или персональные контакты.
"""


def build_messages(city: str, order: list[str], recent_names: list[str]) -> list[dict]:
    return [
        {"role": "system", "content": SYSTEM},
        {"role": "user", "content": (
            f"seed_city = {city}. Значение address.city должно точно совпадать с seed_city.\n"
            f"Выбери текущую специальность из списка: {'; '.join(order)}.\n"
            "Квоты по специальностям не заданы: выбирай разные правдоподобные варианты.\n"
            f"Недавно использованные ФИО, постарайся их не повторять: {', '.join(recent_names) or 'нет'}."
        )},
    ]
