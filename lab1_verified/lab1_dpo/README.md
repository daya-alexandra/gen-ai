# Лабораторная № 1

Реальный прогон выполнен 19.09.2026. Итоговые данные находятся в
`output/stratified/`, сравнительный прогон — в `output/random/`.
Инструкции по проверке и сдаче: [README комплекта](../README.md).
Распаковывать комплект целиком: клиент использует `../common`.

Четыре файла генерации сохранены без изменений относительно хешей в `run.json`.
`verify_results.py` проверяет результаты без API и без перезаписи файлов.
