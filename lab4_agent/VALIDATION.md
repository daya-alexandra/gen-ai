# Локальная проверка

19.09.2026, Python 3.12, Linux. **16 тестов пройдены**, без обращения к DeepSeek или ЦБ. Полный вывод: [local_checks.txt](local_checks.txt).

Проверены все шесть метрик и делегирование get-функциям; отсечение будущих записей; нулевой знаменатель; распространение synthetic; запрет произвольного кода и чрезмерных вычислений; ошибки схемы и инструментов; блокировка бюджета до запроса; отсутствие утечки текста ошибки доступа; возобновление после таймаута и отдельность повторов; конфликт настроек; блокировка одновременного запуска; перенос ключа без чужих лимитов и без перезаписи существующего `.env`.

Интеграционный тест выполняет весь конвейер на явно скриптовом транспорте, проверяет архивные данные против журнала, повторное использование завершённых ответов и обнаружение изменения результата. Фиксированные ответы намеренно не соответствуют большинству gold-вопросов: тест не объявляет их качественными ответами модели.

Сырые API-результаты не получены, итоговые метрики не измерены. Локальные тесты не расходовали пользовательский ключ. Сценарий CMD подготовлен и проверен чтением кода; в этой Linux-среде Windows-сценарий и ветка msvcrt не исполнялись.

CSV сверены с оригинальными Git blob SHA-1, перечисленными в SOURCES.md. Архивы сборки не содержат `.env`, виртуального окружения, временных файлов и тестовых выходов.

